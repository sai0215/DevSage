# You can load these in engine.py instead of hardcoding
translate_prompt_template = "Translate the following request into a bash command: {command}"
log_prompt_template = "Provide a bash command to retrieve logs for the service: {service}"
